
//Carry the one
if countdown_second != 0 && !done_minute    math_minute -= 1;
if countdown_minute != 0 && !done_hour    math_hour -= 1;
if countdown_hour != 0 && !done_day    math_day -= 1;
if countdown_day != 0 && !done_month    math_month -= 1;
if countdown_month != 0 && !done_year    math_year -= 1;
